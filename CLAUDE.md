# CLAUDE.md — دليل Claude Code الرسمي
## مشروع "معجزات القرآن الكريم" | الإصدار 3.0

> هذا الملف يُقرأ تلقائياً في كل جلسة Claude Code.
> اقرأه أولاً، ثم افتح الملف المناسب من `docs/` قبل البدء.

---

## 🎯 الهدف الجوهري

```
بناء أول منصة في العالم تجمع:
  الاستكشاف المستمر الآلي  +  التنبؤ بالمعجزات المحتملة  +  توجيه الباحثين
بالذكاء الاصطناعي — 24/7 — بدون تدخل بشري في الاستكشاف.
```

---

## 🗺️ خريطة الوثائق — افتح الملف الصحيح قبل البدء

```
المهمة                              → الملف المرجعي
─────────────────────────────────────────────────────────────────
بناء LangGraph / وكلاء AI           → docs/02_langgraph_agents.md
System Prompts (بروتوكولات الوكلاء) → docs/03_system_prompts.md
نماذج العربية / RAG / Embeddings    → docs/04_arabic_nlp_rag.md
قاعدة البيانات / SQL Schema         → docs/05_database_schema.md
الارتباطات العلمية / مستويات الثقة  → docs/06_confidence_framework.md
Frontend / UI / Components          → docs/07_frontend_design.md
وحدات العلوم والإنسانيات            → docs/08_analysis_modules.md
خطة التنفيذ / الأسابيع الثمانية     → docs/09_implementation_plan.md
أوامر Claude Code الجاهزة          → docs/10_claude_commands.md
محرك التنبؤ / Predictive Engine    → docs/11_predictive_engine.md
معمارية النظام / Tech Stack         → docs/01_vision_architecture.md
```

---

## ⚡ القواعد الذهبية — لا تبدأ بدون قراءتها

```
القاعدة 1 — النص القرآني مقدس:
  ✅ المصدر الوحيد: tanzil.net
  ✅ لا تُعدّل حرفاً — لا تشكيل — لا تصحيح إملائي
  ✅ كل آية تُخزَّن بثلاث صيغ: text_uthmani + text_simple + text_clean

القاعدة 2 — نظام المستويات إلزامي دائماً:
  ✅ كل ارتباط علمي: tier_1 / tier_2 / tier_3
  ✅ كل فرضية تنبؤية: tier_0 → tier_4
  ✅ الاعتراضات لا تُحذف — تُعرض بجانب الادعاء دائماً

القاعدة 3 — الذكاء الاصطناعي هو القلب:
  ✅ LangGraph يعمل في كل استعلام بدون استثناء
  ✅ SSE Streaming — المستخدم يرى النتائج لحظة بلحظة
  ✅ المحرك المستقل يعمل في الخلفية 24/7

القاعدة 4 — العربية أولاً:
  ✅ dir="rtl" في كل مكوّن frontend
  ✅ خط Scheherazade New للنص القرآني — الحجم ≥ 22px
  ✅ IBM Plex Arabic لواجهة المستخدم
  ✅ لا تخلط Unicode encodings أبداً

القاعدة 5 — الأداء:
  ✅ كل embeddings مسبقة الحساب في قاعدة البيانات
  ✅ Redis لكل شيء يُحسَب أكثر من مرة
  ✅ TTFT < 700ms
```

---

## 🛠️ المجموعة التقنية (Tech Stack)

```yaml
Frontend:
  framework:   Next.js 15  (App Router + TypeScript strict)
  styling:     Tailwind CSS + Shadcn/UI
  ai_client:   Vercel AI SDK (@ai-sdk/anthropic)
  charts:      Recharts + D3.js + Cytoscape.js
  state:       Zustand + TanStack Query
  rtl:         next-intl + tailwindcss-rtl
  realtime:    Yjs (CRDT)
  pwa:         next-pwa

Backend:
  language:    Python 3.12
  framework:   FastAPI 0.111 + uvicorn
  ai_engine:   LangGraph 0.2 + langchain-anthropic
  streaming:   SSE (Server-Sent Events)
  tasks:       Celery + Redis
  scheduler:   APScheduler

Arabic_NLP:
  classical:   CAMeLBERT-CA  (العربية الكلاسيكية — NYU Abu Dhabi)
  semantic:    AraBERT v0.2 Large
  morphology:  CAMeL Tools v1.2.0
  embeddings:  text-embedding-3-large (OpenAI)
  reasoning:   claude-sonnet-4-5 (Anthropic)

Database:
  primary:     PostgreSQL 16 (Supabase)
  vector:      pgvector extension
  graph:       Neo4j 5.x
  cache:       Redis 7 (Upstash)

Deployment:
  frontend:    Vercel
  backend:     Railway
  storage:     Cloudflare R2
  ci_cd:       GitHub Actions
  monitoring:  Sentry + Datadog
```

---

## 📁 هيكل المشروع

```
quran-miracles/
├── CLAUDE.md                    ← أنت هنا
├── docs/                        ← الوثائق التقنية المفصلة
│   ├── 01_vision_architecture.md
│   ├── 02_langgraph_agents.md
│   ├── 03_system_prompts.md
│   ├── 04_arabic_nlp_rag.md
│   ├── 05_database_schema.md
│   ├── 06_confidence_framework.md
│   ├── 07_frontend_design.md
│   ├── 08_analysis_modules.md
│   ├── 09_implementation_plan.md
│   ├── 10_claude_commands.md
│   └── 11_predictive_engine.md
│
├── frontend/                    ← Next.js 15
│   ├── app/
│   │   ├── (quran)/
│   │   ├── (discovery)/
│   │   ├── (sciences)/
│   │   └── (prediction)/        ← محرك التنبؤ
│   ├── components/
│   │   ├── quran/
│   │   ├── discovery/
│   │   └── prediction/
│   └── lib/
│
├── backend/                     ← Python FastAPI
│   ├── discovery_engine/
│   │   ├── core/                ← LangGraph
│   │   ├── agents/              ← الوكلاء الستة
│   │   ├── mcts/                ← MCTS Explorer
│   │   ├── prediction/          ← محرك التنبؤ
│   │   ├── autonomous/          ← المحرك المستقل
│   │   └── prompts/             ← System Prompts
│   ├── arabic_nlp/
│   ├── database/
│   └── api/
│
├── data/
│   ├── quran/                   ← النص القرآني من tanzil.net
│   ├── tafseers/                ← التفاسير (7 مراجع)
│   ├── shaarawy/                ← تفسير الشعراوي (PDF pipeline)
│   └── science/                 ← الأبحاث العلمية
│
├── .github/
│   └── workflows/
│       ├── ci.yml               ← اختبارات تلقائية
│       ├── deploy-frontend.yml  ← Vercel
│       └── deploy-backend.yml   ← Railway
│
└── docker-compose.yml
```

---

## 📚 مراجع التفسير المعتمدة (بالأولوية)

```
1. ابن كثير       → أثري — المرجع الرئيسي
2. الطبري         → أثري — الجمع والترجيح
3. الشعراوي  ★    → بياني لغوي — العمق اللغوي الدقيق
4. الرازي         → عقلي — التحليل المنطقي
5. السعدي         → تيسيري — الفوائد والأحكام
6. ابن عاشور      → إصلاحي — التحليل اللغوي الحديث
7. القرطبي        → فقهي — أحكام القرآن

★ الشعراوي: يُستخدَم بشكل خاص عند:
  - تحليل دقائق الكلمات القرآنية (لماذا هذه الكلمة بالذات؟)
  - الأثر النفسي والاجتماعي للآيات
  - مقارنة الألفاظ المتشابهة في القرآن
```

---

## 🚦 نقطة البداية السريعة

```bash
# الجلسة الأولى — إعداد المشروع
claude "اقرأ docs/01_vision_architecture.md ثم أنشئ هيكل المشروع الكامل"

# الجلسة الثانية — قاعدة البيانات
claude "اقرأ docs/05_database_schema.md ثم نفّذ Schema كاملاً مع pipeline الشعراوي"

# الجلسة الثالثة — محرك LangGraph
claude "اقرأ docs/02_langgraph_agents.md و docs/03_system_prompts.md ثم ابنِ المحرك"

# الجلسة الرابعة — محرك التنبؤ
claude "اقرأ docs/11_predictive_engine.md ثم نفّذ AbductiveReasoningEngine"
```

---

*CLAUDE.md — الإصدار 3.0 | معجزات القرآن الكريم*
