# CI-only requirements — lighter subset for testing
# Full deps in requirements.txt (includes camel-tools, langgraph, etc.)

# === Core Framework ===
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.9.0
pydantic-settings==2.6.0

# === Database ===
sqlalchemy[asyncio]==2.0.35
asyncpg==0.30.0
pgvector==0.3.6
alembic==1.14.0

# === Cache ===
redis==5.2.0

# === SSE Streaming ===
sse-starlette==2.1.0

# === AI / Embeddings ===
openai==1.55.0

# === Utilities ===
httpx==0.28.0
python-dotenv==1.0.1
python-multipart==0.0.12

# === Dev / Testing ===
pytest==8.3.0
pytest-asyncio==0.24.0
ruff==0.8.0
mypy==1.13.0
